from pyfiglet import Figlet
import sys 

user_input = input("Input text to convert to desired font")
if len(figlet) = 1:
    sys.exit

figlet = Figlet()

figlet.getFonts()

figlet.setFont(font=f)
print(figlet.renderText(user_input))
